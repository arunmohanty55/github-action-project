name: Hello Arun Aqua

'on':
  push:
    branches:
      - main

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        run: echo "code clone from github"

  build:
    runs-on: ubuntu-latest
    steps:
      - name: build docker container
        run: echo "docker container"
        
  test: 
   runs-on: ubuntu-latest
   steps:
     - name: test the code
       run: echo "test operation is going on"
       
  deploy:
   runs-on: ubuntu-latest
   steps:
     - name: deployment
       run: echo "deployment is going on"       
